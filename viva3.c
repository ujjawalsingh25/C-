void swap(int *x, int *y)
{
    int temp;
    temp= *x;
    *x= *y;
    *y = temp;
    return;
}
int array(int a[],int b[],int n);
int even=0,i;
int max=x[0],min=x[0],i,diff;
int source[int n], dest[int n]
for(i=0;i<n;i++)
if (source[i]%2 !=0 && dest[i] !==0)
{
    swap(&source[i], &dest[i])
}
for(i=0;i<n;i++){
if (source[i]>max)
{
    max=source[i];
}
if (dest[i]<min)
{
    min= source[i];
} return diff;




void array(int source[], int dest[], int n)
{
    int i, max =a[0], min = a[0];
    for (i=o;i<n;i++)
    {
        if(max<a[i])
        {
            max= a[i];
        }
        if(min >a[i])
        {
            min= a[i];
        }
    }

}